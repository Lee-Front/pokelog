PokeLog의 일반 화면 헤더는 브랜딩보다 현재 맥락과 즉시 행동에 필요한 정보를 우선해야 하고, 고정 HUD는 중요도 높은 정보만 남기고 나머지는 화면별로 맥락적으로 보여주는 편이 좋습니다.

또 PokeLog 자체 설계상 사용자가 계속 확인하게 되는 핵심 상태는 서버, 사용자, 현재 화면, 이벤트, 포인트, 파티/선두 포켓몬 같은 값들이므로 이 범위 안에서 2줄 헤더를 구성하는 것이 적절합니다.

전달용 정리문
일반 화면 헤더 방향
로그인 직후 타이틀 화면에서는 기존처럼 큰 칠색조 아트와 POKELOG 로고를 유지한다.
​

그러나 일반 화면(status, events, party, shop 등)에서는 큰 로고와 그림을 반복해서 노출하지 않는다.

일반 화면은 브랜딩보다 정보 전달과 현재 맥락 인지를 우선한다.

따라서 일반 화면에는 2줄짜리 정보성 헤더를 사용한다.

헤더 설계 원칙
항상 보여야 하는 정보와 화면별로 바뀌는 정보를 구분한다.

헤더에는 사용자가 지금 어디에 있고, 누구로 접속했고, 무엇을 해야 하는지 판단하는 데 필요한 정보만 넣는다.

총 경험치, 도감 수, 마지막 커밋 시간 같은 값은 상시 헤더보다 status 화면에서 보여주는 것이 적절하다.
​

1줄 헤더: 고정 맥락 정보
아래 3개는 모든 일반 화면에서 공통으로 표시한다.

서버명

닉네임

현재 화면명

예시:

text
server: company   trainer: 재혁   screen: status
2줄 헤더: 핵심 상태 정보
아래 항목을 우선 후보로 사용한다.

이벤트 수 (pendingEvents)
​

포인트
​

선두 포켓몬 이름 + 레벨
​

필요 시 전투 중 여부 (battleState)
​

권장 기본안:

text
events: 2   points: 4200   lead: pikachu Lv.15
최종 추천 헤더 형태
text
server: company   trainer: 재혁   screen: party
events: 2   points: 4200   lead: pikachu Lv.15
이 구성이 좋은 이유:

멀티 서버 구조에서 현재 서버를 명확히 인지할 수 있다.
​

사용자 식별이 가능하다.
​

현재 화면 컨텍스트가 분명하다.

게임 핵심 상태인 이벤트/포인트/포켓몬 정보를 바로 확인할 수 있다.
​

로고 없이도 PokeLog다운 감성과 실용성을 함께 유지할 수 있다.

화면별 확장 규칙
2줄째 정보는 화면에 따라 일부 가변적으로 바꿔도 된다.
​

예:

status: 이벤트 수, 포인트, 선두 포켓몬

events: 이벤트 수, 전투 중 여부

party: 파티 수, 선두 포켓몬

shop: 포인트

battle: 내 포켓몬, 상대 포켓몬, 턴 상태

즉 1줄은 고정, 2줄은 화면별로 조정하는 구조를 권장한다.

제외 권장 항목
다음 값들은 항상 헤더에 넣기보다는 상세 화면에서 보여주는 것이 좋다.

총 경험치

도감 수

마지막 커밋 시각

콤보 배수

세부 인벤토리 수치

이 값들은 정보량은 많지만 모든 화면에서 즉시 행동 판단에 꼭 필요하지는 않다.
​
​

결정 사항
일반 화면에는 POKELOG 큰 로고를 반복하지 않는다.

일반 화면에는 2줄 헤더를 적용한다.

1줄은 서버명 / 닉네임 / 현재 화면.

2줄은 이벤트 수 / 포인트 / 선두 포켓몬을 기본으로 한다.

화면 특성에 따라 2줄째 일부 항목은 교체 가능하다.

타이틀 브랜딩은 로그인 직후 첫 화면에서만 강하게 보여준다.